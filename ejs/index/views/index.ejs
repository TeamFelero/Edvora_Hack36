<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Leapp for Colleges</title>
    <link rel="stylesheet" href="style.css">
    <script src="https://www.paypal.com/sdk/js?client-id=sb&currency=USD"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.1/gsap.min.js"></script>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <style>
        .nav-link {
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-link:hover {
            transform: translateY(-2px);
        }

        .nav-link::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -2px;
            left: 0;
            background-color: #ec4899;
            transition: width 0.3s ease;
        }

        .nav-link:hover::after {
            width: 100%;
        }

        .college-card {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            transform-origin: center;
        }

        .college-card:hover {
            transform: translateY(-4px) scale(1.01);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        }

        .review-card {
            transition: all 0.3s ease;
        }

        .review-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }

        /* Add these styles */
        .transition-all {
            transition: all 0.5s ease-in-out;
        }

        .sidebar {
            transform: translateX(-100%);
        }

        .sidebar.open {
            transform: translateX(0);
        }

        .main-content {
            transform: translateX(0);
        }

        .main-content.collapsed {
            transform: translateX(100%);
        }

        .about-content {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            pointer-events: none;
            z-index: 10;
            transition: opacity 0.3s ease;
        }

        .about-content.open {
            opacity: 1;
            pointer-events: auto;
        }

        #main-content,
        #about-content {
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

        #main-content {
            transform: translateX(0);
            opacity: 1;
        }

        #main-content.hidden {
            transform: translateX(-20px);
            opacity: 0;
            position: absolute;
            pointer-events: none;
        }

        #about-content {
            transform: translateX(20px);
            opacity: 0;
            position: absolute;
        }

        #about-content:not(.hidden) {
            transform: translateX(0);
            opacity: 1;
            position: relative;
        }
    </style>
</head>

<body class="m-0 font-sans bg-gradient-to-r from-[#121212] to-[#1e1e1e] text-white">

    <!-- Navbar -->
    <nav
        class="bg-[#c0c0c0]/20 backdrop-blur-lg shadow-xl rounded-3xl max-w-[90%] mt-6 mx-auto px-6 mb-4 border border-white/20 transition-all duration-300 hover:shadow-2xl">
        <div class="max-w-7xl mx-auto py-3 flex items-center justify-between">
            <!-- Logo - Enhanced with animation -->
            <div class="flex items-center space-x-3 group">
                <img src="./assets/logo2.png" alt="Logo"
                    class="h-16 drop-shadow-lg transition-transform duration-300 group-hover:scale-110">
                <div
                    class="text-3xl font-bold bg-gradient-to-r from-pink-500 via-purple-500 to-indigo-500 bg-clip-text text-transparent animate-gradient-x">
                    Edvora
                </div>
            </div>

            <!-- Desktop Menu - Improved spacing and effects -->
            <div class="hidden md:flex space-x-10 items-center">
                <a href="#" class="nav-link text-white/90 hover:text-white text-lg font-medium relative group">
                    Home
                    <span
                        class="absolute bottom-0 left-0 w-0 h-0.5 bg-pink-400 transition-all duration-300 group-hover:w-full"></span>
                </a>
                <a href="#"
                    class="nav-link text-white/90 hover:text-white text-lg font-medium relative group about-link">
                    About
                    <span
                        class="absolute bottom-0 left-0 w-0 h-0.5 bg-purple-400 transition-all duration-300 group-hover:w-full"></span>
                </a>
                <% if(isAuthenticated) { %>
                    <a href="/myProfile"
                        class="nav-link px-5 py-2 rounded-full bg-gradient-to-r from-pink-500 to-purple-600 text-white font-medium hover:from-pink-600 hover:to-purple-700 transition-all shadow-lg hover:shadow-pink-500/30">
                        <%= user.username %>
                    </a>
                    <a href="/logout"
                        class="nav-link px-5 py-2 rounded-full bg-gradient-to-r from-pink-500 to-purple-600 text-white font-medium hover:from-pink-600 hover:to-purple-700 transition-all shadow-lg hover:shadow-pink-500/30">
                        Logout
                    </a>
                    <% } else { %>
                        <a href="/login"
                            class="nav-link px-5 py-2 rounded-full bg-gradient-to-r from-pink-500 to-purple-600 text-white font-medium hover:from-pink-600 hover:to-purple-700 transition-all shadow-lg hover:shadow-pink-500/30">
                            Login
                        </a>
                        <% } %>
            </div>

            <!-- Hamburger - More prominent -->
            <div class="md:hidden">
                <button id="menu-toggle"
                    class="p-2 rounded-xl hover:bg-white/20 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-purple-500">
                    <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" stroke-width="2"
                        viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>

        <!-- Mobile Menu - Enhanced styling -->
        <div id="mobile-menu"
            class="hidden md:hidden px-4 pb-6 pt-2 flex flex-col space-y-4 bg-white/5 rounded-b-2xl backdrop-blur-lg">
            <a href="#"
                class="nav-link text-white py-3 px-5 rounded-lg hover:bg-white/10 transition-all flex items-center group">
                <span
                    class="w-1.5 h-1.5 bg-pink-400 rounded-full mr-3 opacity-0 group-hover:opacity-100 transition-opacity"></span>
                Home
            </a>
            <a href="#"
                class="nav-link text-white py-3 px-5 rounded-lg hover:bg-white/10 transition-all flex items-center group about-link">
                <span
                    class="w-1.5 h-1.5 bg-purple-400 rounded-full mr-3 opacity-0 group-hover:opacity-100 transition-opacity"></span>
                About
            </a>
            <a href="/login"
                class="nav-link text-white py-3 px-5 rounded-lg bg-gradient-to-r from-pink-500/80 to-purple-600/80 hover:from-pink-500 hover:to-purple-600 transition-all mt-2 text-center font-medium">
                Login
            </a>
        </div>
    </nav>
    <!-- Page Content -->
    <div class="flex flex-col md:flex-row min-h-screen">

        <!-- Add this inside your flex container (where your main content is) -->
        <div id="about-content"
            class="hidden flex-1 bg-gray-50 text-gray-800 p-8 rounded-t-[30px] md:rounded-l-[30px] w-full shadow-inner overflow-y-auto">
            <div class="max-w-4xl mx-auto">
                <!-- Back Button -->
                <button id="back-button"
                    class="mb-8 px-6 py-3 bg-gradient-to-r from-gray-200 to-gray-300 text-gray-700 rounded-xl font-medium hover:from-gray-300 hover:to-gray-400 transition-all flex items-center gap-2 shadow-sm">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd"
                            d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z"
                            clip-rule="evenodd" />
                    </svg>
                    Back to Main
                </button>

                <!-- About Content -->
                <div class="bg-white p-8 md:p-10 rounded-2xl shadow-lg border border-gray-100">
                    <h1
                        class="text-4xl font-bold text-center mb-10 bg-gradient-to-r from-pink-500 to-purple-600 bg-clip-text text-transparent">
                        About Edvora</h1>

                    <!-- Problem Section -->
                    <div class="mb-12">
                        <div class="flex items-center gap-3 mb-4">
                            <div class="w-3 h-8 rounded-full bg-red-500"></div>
                            <h2 class="text-2xl font-bold text-gray-800">The Problem We Solve</h2>
                        </div>
                        <p class="text-gray-600 leading-relaxed">
                            Choosing the right college is one of the most important decisions in a student's life, yet
                            the process is often overwhelming and confusing. Students struggle with:
                        </p>
                        <ul class="mt-4 space-y-3 pl-5">
                            <li class="flex items-start gap-2">
                                <span class="text-pink-500 mt-1">•</span>
                                <span class="text-gray-600">Limited access to authentic, up-to-date information about
                                    colleges</span>
                            </li>
                            <li class="flex items-start gap-2">
                                <span class="text-pink-500 mt-1">•</span>
                                <span class="text-gray-600">Difficulty comparing colleges based on personal
                                    priorities</span>
                            </li>
                            <li class="flex items-start gap-2">
                                <span class="text-pink-500 mt-1">•</span>
                                <span class="text-gray-600">Lack of insights from current students and alumni</span>
                            </li>
                            <li class="flex items-start gap-2">
                                <span class="text-pink-500 mt-1">•</span>
                                <span class="text-gray-600">Time-consuming research across multiple sources</span>
                            </li>
                        </ul>
                    </div>

                    <!-- Solution Section -->
                    <div class="mb-12">
                        <div class="flex items-center gap-3 mb-4">
                            <div class="w-3 h-8 rounded-full bg-green-500"></div>
                            <h2 class="text-2xl font-bold text-gray-800">Our Solution</h2>
                        </div>
                        <p class="text-gray-600 leading-relaxed mb-4">
                            Edvora revolutionizes college discovery by combining AI-powered recommendations with
                            authentic student experiences:
                        </p>
                        <div class="grid md:grid-cols-2 gap-6 mt-6">
                            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                                <div class="text-pink-500 mb-3">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
                                    </svg>
                                </div>
                                <h3 class="font-bold text-lg text-gray-800 mb-2">Smart Matching</h3>
                                <p class="text-gray-600 text-sm">Our AI analyzes your preferences to recommend colleges
                                    that truly fit your needs.</p>
                            </div>
                            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                                <div class="text-purple-500 mb-3">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a1.994 1.994 0 01-1.414-.586m0 0L11 14h4a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2v4l.586-.586z" />
                                    </svg>
                                </div>
                                <h3 class="font-bold text-lg text-gray-800 mb-2">Real Reviews</h3>
                                <p class="text-gray-600 text-sm">Get honest insights from students who've been there.
                                </p>
                            </div>
                            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                                <div class="text-blue-500 mb-3">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                                    </svg>
                                </div>
                                <h3 class="font-bold text-lg text-gray-800 mb-2">Comprehensive Data</h3>
                                <p class="text-gray-600 text-sm">All the information you need in one place - fees,
                                    placements, facilities and more.</p>
                            </div>
                            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                                <div class="text-indigo-500 mb-3">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z" />
                                    </svg>
                                </div>
                                <h3 class="font-bold text-lg text-gray-800 mb-2">Live Updates</h3>
                                <p class="text-gray-600 text-sm">Real-time information through our integrated APIs and
                                    community.</p>
                            </div>
                        </div>
                    </div>

                    <!-- Team Section -->
                    <div class="mb-12">
                        <div class="flex items-center gap-3 mb-6">
                            <div class="w-3 h-8 rounded-full bg-blue-500"></div>
                            <h2 class="text-2xl font-bold text-gray-800">Meet The Team</h2>
                        </div>
                        <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
                            <div class="text-center">
                                <div
                                    class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-r from-pink-500 to-purple-600 p-1">
                                    <div class="bg-white w-full h-full rounded-full p-1">
                                        <img src="https://via.placeholder.com/128" alt="Team Member"
                                            class="w-full h-full rounded-full object-cover">
                                    </div>
                                </div>
                                <h3 class="font-bold text-lg">Yug Shah</h3>
                                <p class="text-gray-600">20243255</p>
                                <p class="text-sm text-pink-600 font-medium">Frontend Developer</p>
                            </div>
                            <!-- Repeat for other team members -->
                            <div class="text-center">
                                <div
                                    class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-r from-blue-500 to-teal-400 p-1">
                                    <div class="bg-white w-full h-full rounded-full p-1">
                                        <img src="https://via.placeholder.com/128" alt="Team Member"
                                            class="w-full h-full rounded-full object-cover">
                                    </div>
                                </div>
                                <h3 class="font-bold text-lg">Neel Mendapara</h3>
                                <p class="text-gray-600">20243175</p>
                                <p class="text-sm text-blue-600 font-medium">Backend Developer</p>
                            </div>
                            <div class="text-center">
                                <div
                                    class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-r from-purple-500 to-indigo-600 p-1">
                                    <div class="bg-white w-full h-full rounded-full p-1">
                                        <img src="https://via.placeholder.com/128" alt="Team Member"
                                            class="w-full h-full rounded-full object-cover">
                                    </div>
                                </div>
                                <h3 class="font-bold text-lg">Yagnik Chavda</h3>
                                <p class="text-gray-600">20243081</p>
                                <p class="text-sm text-purple-600 font-medium">UI/UX Designer</p>
                            </div>
                            <div class="text-center">
                                <div
                                    class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-r from-green-500 to-emerald-600 p-1">
                                    <div class="bg-white w-full h-full rounded-full p-1">
                                        <img src="https://via.placeholder.com/128" alt="Team Member"
                                            class="w-full h-full rounded-full object-cover">
                                    </div>
                                </div>
                                <h3 class="font-bold text-lg">Bhargav Miyani</h3>
                                <p class="text-gray-600">20247029</p>
                                <p class="text-sm text-green-600 font-medium">Full Stack Developer</p>
                            </div>
                        </div>
                    </div>

                    <!-- Call to Action -->
                    <div class="text-center mt-16">
                        <h3 class="text-xl font-bold text-gray-800 mb-4">Ready to find your perfect college?</h3>
                        <button id="back-to-main"
                            class="px-8 py-3 bg-gradient-to-r from-pink-500 to-purple-600 text-white rounded-xl font-bold hover:from-pink-600 hover:to-purple-700 transition-all shadow-lg">
                            Start Exploring Now
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <script>
            document.addEventListener('DOMContentLoaded', function () {
                const mainContent = document.getElementById('main-content');
                const aboutContent = document.getElementById('about-content');

                // Show About Section
                document.querySelectorAll('.about-link').forEach(link => {
                    link.addEventListener('click', function (e) {
                        e.preventDefault();
                        mainContent.classList.add('hidden');
                        aboutContent.classList.remove('hidden');
                    });
                });

                // Back buttons
                document.querySelectorAll('#back-button, #back-to-main').forEach(button => {
                    button.addEventListener('click', function (e) {
                        e.preventDefault();
                        mainContent.classList.remove('hidden');
                        aboutContent.classList.add('hidden');
                    });
                });
            });
        </script>

        <!-- Sidebar -->
        <aside class="w-full md:w-[400px] p-8 md:p-12 bg-gradient-to-b from-black to-gray-900">
            <h2 class="text-4xl text-gray-300 leading-snug font-bold">
                Find<br>Suitable <span class="text-gray-400">Collage</span><br>
                <strong class="text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-purple-500">with
                    AI</strong>
            </h2>
            <p class="text-gray-400 mt-6 mb-8 leading-relaxed">
                Edvora is here to help guide you to your dream place...
            </p>
            <a href="#"
                class="inline-block bg-gradient-to-r from-pink-500 to-purple-600 text-white px-8 py-3 rounded-xl font-bold transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-pink-500/20 nav-link"
                data-section="AI">Ask EdvoraAI</a>
        </aside>

        <!-- Main Content -->
        <main id="main-content"
            class="flex-1 bg-gray-50 text-gray-800 p-8 rounded-t-[30px] md:rounded-l-[30px] w-full shadow-inner">
            <!-- Top Nav -->
            <header class="flex flex-wrap justify-between gap-4 mb-8">
                <nav class="flex flex-wrap gap-4 justify-start items-center w-full">
                    <a href="#" class="px-4 py-2 rounded-full bg-pink-100 text-pink-700 font-medium shadow-sm"
                        data-section="Colleges">Colleges</a>
                    <a href="#"
                        class="px-4 py-2 rounded-full bg-gray-200 text-gray-700 font-medium hover:bg-gray-300 transition-all"
                        data-section="News">News</a>
                    <a href="#"
                        class="px-4 py-2 rounded-full bg-gray-200 text-gray-700 font-medium hover:bg-gray-300 transition-all"
                        data-section="Reviews">Reviews</a>
                    <a href="#"
                        class="px-4 py-2 rounded-full bg-gray-200 text-gray-700 font-medium hover:bg-gray-300 transition-all"
                        data-section="Chat">Chat</a>
                    <a href="#"
                        class="ml-auto px-6 py-3 rounded-full bg-gradient-to-r from-yellow-400 via-pink-500 to-red-500 text-white font-bold text-lg shadow-xl hover:scale-105 hover:shadow-2xl transition-all duration-300 backdrop-blur-lg border border-white/20"
                        data-section="GetPremium">✨ Get Premium</a>
                </nav>
            </header>

            <!-- colleges -->
            <div id="city-search-wrapper">
                <section class="mb-8" id="search-section">
                    <div class="relative w-full">
                        <input id="cityInput" type="text" placeholder="Search for colleges in any city..."
                            class="w-full py-4 pl-5 pr-14 rounded-xl border-none focus:ring-2 focus:ring-pink-400 focus:outline-none text-gray-800 shadow-lg" />
                        <button onclick="fetchColleges()"
                            class="absolute top-1/2 right-4 transform -translate-y-1/2 bg-gradient-to-r from-pink-500 to-purple-600 hover:from-pink-600 hover:to-purple-700 p-2.5 rounded-full transition-all duration-300 shadow-md">
                            <img src="./assets/search.png" alt="Search" class="h-5 w-5" />
                        </button>
                    </div>
                </section>

                <section class="mb-8" id="result"></section>

                <section class="h-[500px] w-full rounded-2xl overflow-hidden mb-8 border-2 border-white shadow-xl"
                    id="map-section">
                    <div id="map" class="w-full h-full"></div>
                </section>
            </div>

            <!-- news -->
            <section class="p-6 bg-gray-100 text-gray-800 hidden" id="news-section">
                <div class="max-w-6xl mx-auto">
                    <h2 class="text-2xl font-bold mb-6">Latest News & Articles</h2>
                    <div id="news-container" class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                        <!-- News cards will be loaded here -->
                        <div class="text-center py-10">
                            <div
                                class="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-pink-500 mx-auto mb-4">
                            </div>
                            <p>Loading news articles...</p>
                        </div>
                    </div>
                </div>
            </section>

            <!-- reviews -->
            <section id="Review" class="p-6 bg-gray-100 text-gray-800 hidden">
                


                <div class="px-4 py-10">
                    <h2
                        class="text-3xl font-bold mb-10 text-center bg-gradient-to-r from-pink-500 to-purple-600 bg-clip-text text-transparent">
                        Student Reviews</h2>

                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
                        <!-- Review Card -->
                        <div
                            class="review-card bg-white rounded-2xl shadow-xl p-6 flex flex-col items-center text-center border border-gray-100">
                            <div class="mb-4">
                                <img src="https://via.placeholder.com/100" alt="User Photo"
                                    class="w-24 h-24 rounded-full object-cover border-4 border-pink-200 shadow-md">
                            </div>
                            <h3 class="text-xl font-semibold text-gray-800">Ananya Sharma</h3>
                            <p class="text-sm text-pink-600 font-medium">IIT Delhi</p>
                            <p class="text-sm text-gray-600 mt-2">Mathematics</p>
                            <div class="flex justify-center mt-3">
                                <span class="text-yellow-400 text-xl">★★★★☆</span>
                            </div>
                            <p class="mt-4 text-gray-600 text-sm">"Great faculty and infrastructure. Placements are
                                excellent!"</p>
                        </div>

                        <!-- Repeat cards -->
                        <div
                            class="review-card bg-white rounded-2xl shadow-xl p-6 flex flex-col items-center text-center border border-gray-100">
                            <div class="mb-4">
                                <img src="https://via.placeholder.com/100" alt="User Photo"
                                    class="w-24 h-24 rounded-full object-cover border-4 border-blue-200 shadow-md">
                            </div>
                            <h3 class="text-xl font-semibold text-gray-800">Rohan Mehta</h3>
                            <p class="text-sm text-blue-600 font-medium">BITS Pilani</p>
                            <p class="text-sm text-gray-600 mt-2">Physics</p>
                            <div class="flex justify-center mt-3">
                                <span class="text-yellow-400 text-xl">★★★★★</span>
                            </div>
                            <p class="mt-4 text-gray-600 text-sm">"Amazing research opportunities and campus life."</p>
                        </div>

                        <!-- Another card -->
                        <div
                            class="review-card bg-white rounded-2xl shadow-xl p-6 flex flex-col items-center text-center border border-gray-100">
                            <div class="mb-4">
                                <img src="https://via.placeholder.com/100" alt="User Photo"
                                    class="w-24 h-24 rounded-full object-cover border-4 border-purple-200 shadow-md">
                            </div>
                            <h3 class="text-xl font-semibold text-gray-800">Priya Verma</h3>
                            <p class="text-sm text-purple-600 font-medium">MNNIT Allahabad</p>
                            <p class="text-sm text-gray-600 mt-2">Chemistry</p>
                            <div class="flex justify-center mt-3">
                                <span class="text-yellow-400 text-xl">★★★☆☆</span>
                            </div>
                            <p class="mt-4 text-gray-600 text-sm">"Good academics but could improve hostel facilities."
                            </p>
                        </div>
                    </div>
                </div>
                <a href="#"
                class="inline-block bg-gradient-to-r from-pink-500 to-purple-600 text-white px-8 py-3 rounded-xl font-bold transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-pink-500/20 nav-link"
                data-section="AI">Add REview</a>
            </section>

            <!-- GetPremium -->
            <section id="GetPremium" class="p-6 bg-gray-100 text-gray-800 hidden">
                <div
                    class="min-h-[80vh] bg-gradient-to-br from-gray-900 to-black text-white flex flex-col items-center justify-center px-4 py-8 rounded-3xl border border-gray-800 shadow-2xl">
                    <div class="text-center max-w-2xl">
                        <h1
                            class="text-4xl md:text-5xl font-bold mb-4 bg-gradient-to-r from-yellow-400 via-pink-500 to-red-500 bg-clip-text text-transparent">
                            College Finder Premium</h1>
                        <p class="text-xl text-gray-300 mb-8">Unlock advanced college matching features for just Rs.
                            499/-</p>
                        <div class="bg-gray-800/50 p-8 rounded-xl border border-gray-700 backdrop-blur-sm">
                            <ul class="text-left space-y-3 mb-8 text-gray-300">
                                <li class="flex items-center"><span class="text-pink-400 mr-2">✓</span> Personalized
                                    college recommendations</li>
                                <li class="flex items-center"><span class="text-pink-400 mr-2">✓</span> Advanced
                                    filtering options</li>
                                <li class="flex items-center"><span class="text-pink-400 mr-2">✓</span> Priority support
                                </li>
                                <li class="flex items-center"><span class="text-pink-400 mr-2">✓</span> Exclusive
                                    content and guides</li>
                            </ul>
                            <div id="paypal-button-container" class="mt-6"></div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- chat -->
            <section id="Chat" class="p-6 bg-gray-100 text-gray-800 hidden">
                <% if (!user) { %>
                    <div
                        class="max-w-3xl mx-auto mt-8 bg-white p-6 rounded-2xl shadow-xl border border-gray-200 text-center">
                        <div class="text-2xl font-bold text-pink-600 mb-4">Live Chat</div>
                        <p class="text-gray-600 text-lg">Please <a href="/login"
                                class="text-pink-500 underline hover:text-pink-600">log in</a> to access the chat
                            feature.</p>
                    </div>
                    <% } else { %>
                        <div id="chat-container"
                            class="max-w-3xl mx-auto mt-8 bg-white p-6 rounded-2xl shadow-xl border border-gray-200">
                            <div class="flex items-center mb-6">
                                <div class="bg-pink-500 w-3 h-8 rounded-full mr-3"></div>
                                <h2 class="text-2xl font-bold text-gray-800">Live Chat</h2>
                            </div>
                            <ul id="messages" class="list-none p-0 max-h-96 overflow-y-auto mb-5 space-y-3">
                                <!-- Messages will go here -->
                                <li class="text-center text-gray-500 py-4">Start chatting with other students...</li>
                            </ul>
                            <form id="form" autocomplete="off" class="flex gap-3 mt-6">
                                <input id="input" type="text" placeholder="Type your message..."
                                    class="flex-1 p-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-pink-400 focus:border-pink-400 outline-none shadow-sm" />
                                <button
                                    class="px-6 py-3 bg-gradient-to-r from-pink-500 to-purple-600 text-white rounded-xl font-bold hover:from-pink-600 hover:to-purple-700 focus:outline-none transition-all duration-300 shadow-md">
                                    Send
                                </button>
                            </form>
                        </div>
                        <% } %>
            </section>


            <!-- AI -->
            <section id="AI" class="p-6 bg-gray-100 text-gray-800 hidden">
                <div class="max-w-7xl mx-auto">
                    <h1 class="text-4xl font-bold text-center text-gray-800 mb-10">
                        <span class="bg-gradient-to-r from-blue-600 to-purple-600 text-transparent bg-clip-text">
                            College Recommender
                        </span>
                    </h1>

                    <form id="prefForm"
                        class="max-w-5xl mx-auto p-8 rounded-2xl shadow-xl space-y-8 bg-white border border-gray-200">
                        <!-- First row: Stream, State, Max Fee -->
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                            <label class="flex flex-col">
                                <span class="mb-2 text-gray-600 font-semibold flex items-center">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-pink-500"
                                        viewBox="0 0 20 20" fill="currentColor">
                                        <path
                                            d="M10.394 2.08a1 1 0 00-.788 0l-7 3a1 1 0 000 1.84L5.25 8.051a.999.999 0 01.356-.257l4-1.714a1 1 0 11.788 1.838L7.667 9.088l1.94.831a1 1 0 00.787 0l7-3a1 1 0 000-1.838l-7-3zM3.31 9.397L5 10.12v4.102a8.969 8.969 0 00-1.05-.174 1 1 0 01-.89-.89 11.115 11.115 0 01.25-3.762zM9.3 16.573A9.026 9.026 0 007 14.935v-3.957l1.818.78a3 3 0 002.364 0l5.508-2.361a11.026 11.026 0 01.25 3.762 1 1 0 01-.89.89 8.968 8.968 0 00-5.35 2.524 1 1 0 01-1.4 0zM6 18a1 1 0 001-1v-2.065a8.935 8.935 0 00-2-.712V17a1 1 0 001 1z" />
                                    </svg>
                                    Stream:
                                </span>
                                <input type="text" id="stream" value="Engineering" aria-label="Stream"
                                    class="p-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all" />
                            </label>

                            <label class="flex flex-col">
                                <span class="mb-2 text-gray-600 font-semibold flex items-center">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-blue-500"
                                        viewBox="0 0 20 20" fill="currentColor">
                                        <path fill-rule="evenodd"
                                            d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z"
                                            clip-rule="evenodd" />
                                    </svg>
                                    State:
                                </span>
                                <input type="text" id="state" aria-label="State"
                                    class="p-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all" />
                            </label>

                            <label class="flex flex-col">
                                <span class="mb-2 text-gray-600 font-semibold flex items-center">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-purple-500"
                                        viewBox="0 0 20 20" fill="currentColor">
                                        <path
                                            d="M8.433 7.418c.155-.103.346-.196.567-.267v1.698a2.305 2.305 0 01-.567-.267C8.07 8.34 8 8.114 8 8c0-.114.07-.34.433-.582zM11 12.849v-1.698c.22.071.412.164.567.267.364.243.433.468.433.582 0 .114-.07.34-.433.582a2.305 2.305 0 01-.567.267z" />
                                        <path fill-rule="evenodd"
                                            d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-13a1 1 0 10-2 0v.092a4.535 4.535 0 00-1.676.662C6.602 6.234 6 7.009 6 8c0 .99.602 1.765 1.324 2.246.48.32 1.054.545 1.676.662v1.941c-.391-.127-.68-.317-.843-.504a1 1 0 10-1.51 1.31c.562.649 1.413 1.076 2.353 1.253V15a1 1 0 102 0v-.092a4.535 4.535 0 001.676-.662C13.398 13.766 14 12.991 14 12c0-.99-.602-1.765-1.324-2.246A4.535 4.535 0 0011 9.092V7.151c.391.127.68.317.843.504a1 1 0 101.511-1.31c-.563-.649-1.413-1.076-2.354-1.253V5z"
                                            clip-rule="evenodd" />
                                    </svg>
                                    Max Fee (₹):
                                </span>
                                <input type="number" id="maxFee" aria-label="Max Fee"
                                    class="p-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all" />
                            </label>
                        </div>

                        <!-- Second row: Priorities with dynamic progress bars -->
                        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
                            <label class="flex flex-col">
                                <span class="mb-2 text-gray-600 font-semibold">Placement Priority (1-10):</span>
                                <input type="number" id="placement" value="5" min="1" max="10"
                                    class="p-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all priority-input" />
                                <div class="w-full bg-gray-200 rounded-full h-2.5 mt-2">
                                    <div id="placement-bar" class="bg-pink-500 h-2.5 rounded-full" style="width: 50%">
                                    </div>
                                </div>
                            </label>

                            <label class="flex flex-col">
                                <span class="mb-2 text-gray-600 font-semibold">Infrastructure Priority (1-10):</span>
                                <input type="number" id="infrastructure" value="5" min="1" max="10"
                                    class="p-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all priority-input" />
                                <div class="w-full bg-gray-200 rounded-full h-2.5 mt-2">
                                    <div id="infrastructure-bar" class="bg-blue-500 h-2.5 rounded-full"
                                        style="width: 50%"></div>
                                </div>
                            </label>

                            <label class="flex flex-col">
                                <span class="mb-2 text-gray-600 font-semibold">Faculty Priority (1-10):</span>
                                <input type="number" id="faculty" value="5" min="1" max="10"
                                    class="p-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all priority-input" />
                                <div class="w-full bg-gray-200 rounded-full h-2.5 mt-2">
                                    <div id="faculty-bar" class="bg-purple-500 h-2.5 rounded-full" style="width: 50%">
                                    </div>
                                </div>
                            </label>

                            <label class="flex flex-col">
                                <span class="mb-2 text-gray-600 font-semibold">Academic Priority (1-10):</span>
                                <input type="number" id="academic" value="5" min="1" max="10"
                                    class="p-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all priority-input" />
                                <div class="w-full bg-gray-200 rounded-full h-2.5 mt-2">
                                    <div id="academic-bar" class="bg-green-500 h-2.5 rounded-full" style="width: 50%">
                                    </div>
                                </div>
                            </label>
                        </div>

                        <!-- Submit button -->
                        <button type="submit"
                            class="w-full bg-gradient-to-r from-blue-600 to-purple-600 text-white py-4 rounded-xl font-bold hover:from-blue-700 hover:to-purple-700 transition-all transform hover:scale-[1.01] shadow-lg flex items-center justify-center gap-2">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                            </svg>
                            Find Colleges
                        </button>
                    </form>

                    <div id="results" class="max-w-5xl mx-auto mt-12 grid gap-8">
                        <!-- Results will appear here -->
                    </div>
                </div>

                <script>
                    // Add event listeners when DOM is loaded
                    document.addEventListener('DOMContentLoaded', function () {
                        // Function to update progress bars
                        function updateProgressBar(input) {
                            const value = Math.min(Math.max(input.value, 1), 10); // Clamp between 1-10
                            const id = input.id;
                            const progressBar = document.getElementById(`${id}-bar`);

                            // Calculate percentage (1-10 becomes 10%-100%)
                            const percentage = (value / 10) * 100;

                            // Update the progress bar width
                            if (progressBar) {
                                progressBar.style.width = `${percentage}%`;
                            }
                        }

                        // Set up event listeners for all priority inputs
                        document.querySelectorAll('.priority-input').forEach(input => {
                            // Update when value changes
                            input.addEventListener('input', function () {
                                updateProgressBar(this);
                            });

                            // Initialize on page load
                            updateProgressBar(input);
                        });
                    });
                </script>
            </section>
            <script>
                // Add event listeners to all priority inputs
                document.querySelectorAll('.priority-input').forEach(input => {
                    // Update on input change
                    input.addEventListener('input', function () {
                        updateProgressBar(this);
                    });

                    // Also update when page loads
                    updateProgressBar(input);
                });

                function updateProgressBar(input) {
                    const value = input.value;
                    const id = input.id;
                    const progressBar = document.getElementById(`${id}-bar`);

                    // Calculate percentage (1-10 becomes 10%-100%)
                    const percentage = (value / 10) * 100;

                    // Update the progress bar width
                    if (progressBar) {
                        progressBar.style.width = `${percentage}%`;
                    }

                    // Ensure value stays within bounds
                    if (value > 10) {
                        input.value = 10;
                    } else if (value < 1) {
                        input.value = 1;
                    }
                }
            </script>
        </main>
        <div id="about-content"
            class="hidden flex-1 bg-gray-50 text-gray-800 p-8 rounded-t-[30px] md:rounded-l-[30px] w-full shadow-inner overflow-y-auto">
            <div class="max-w-4xl mx-auto">
                <!-- Back Button -->
                <button id="back-button"
                    class="mb-8 px-6 py-3 bg-gradient-to-r from-gray-200 to-gray-300 text-gray-700 rounded-xl font-medium hover:from-gray-300 hover:to-gray-400 transition-all flex items-center gap-2 shadow-sm">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd"
                            d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z"
                            clip-rule="evenodd" />
                    </svg>
                    Back to Main
                </button>

                <!-- About Content -->
                <div class="bg-white p-8 md:p-10 rounded-2xl shadow-lg border border-gray-100">
                    <h1
                        class="text-4xl font-bold text-center mb-10 bg-gradient-to-r from-pink-500 to-purple-600 bg-clip-text text-transparent">
                        About Edvora</h1>

                    <!-- Problem Section -->
                    <div class="mb-12">
                        <div class="flex items-center gap-3 mb-4">
                            <div class="w-3 h-8 rounded-full bg-red-500"></div>
                            <h2 class="text-2xl font-bold text-gray-800">The Problem We Solve</h2>
                        </div>
                        <p class="text-gray-600 leading-relaxed">
                            Choosing the right college is one of the most important decisions in a student's life, yet
                            the process is often overwhelming and confusing. Students struggle with:
                        </p>
                        <ul class="mt-4 space-y-3 pl-5">
                            <li class="flex items-start gap-2">
                                <span class="text-pink-500 mt-1">•</span>
                                <span class="text-gray-600">Limited access to authentic, up-to-date information about
                                    colleges</span>
                            </li>
                            <li class="flex items-start gap-2">
                                <span class="text-pink-500 mt-1">•</span>
                                <span class="text-gray-600">Difficulty comparing colleges based on personal
                                    priorities</span>
                            </li>
                            <li class="flex items-start gap-2">
                                <span class="text-pink-500 mt-1">•</span>
                                <span class="text-gray-600">Lack of insights from current students and alumni</span>
                            </li>
                            <li class="flex items-start gap-2">
                                <span class="text-pink-500 mt-1">•</span>
                                <span class="text-gray-600">Time-consuming research across multiple sources</span>
                            </li>
                        </ul>
                    </div>

                    <!-- Solution Section -->
                    <div class="mb-12">
                        <div class="flex items-center gap-3 mb-4">
                            <div class="w-3 h-8 rounded-full bg-green-500"></div>
                            <h2 class="text-2xl font-bold text-gray-800">Our Solution</h2>
                        </div>
                        <p class="text-gray-600 leading-relaxed mb-4">
                            Edvora revolutionizes college discovery by combining AI-powered recommendations with
                            authentic student experiences:
                        </p>
                        <div class="grid md:grid-cols-2 gap-6 mt-6">
                            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                                <div class="text-pink-500 mb-3">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
                                    </svg>
                                </div>
                                <h3 class="font-bold text-lg text-gray-800 mb-2">Smart Matching</h3>
                                <p class="text-gray-600 text-sm">Our AI analyzes your preferences to recommend colleges
                                    that truly fit your needs.</p>
                            </div>
                            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                                <div class="text-purple-500 mb-3">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a1.994 1.994 0 01-1.414-.586m0 0L11 14h4a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2v4l.586-.586z" />
                                    </svg>
                                </div>
                                <h3 class="font-bold text-lg text-gray-800 mb-2">Real Reviews</h3>
                                <p class="text-gray-600 text-sm">Get honest insights from students who've been there.
                                </p>
                            </div>
                            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                                <div class="text-blue-500 mb-3">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                                    </svg>
                                </div>
                                <h3 class="font-bold text-lg text-gray-800 mb-2">Comprehensive Data</h3>
                                <p class="text-gray-600 text-sm">All the information you need in one place - fees,
                                    placements, facilities and more.</p>
                            </div>
                            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                                <div class="text-indigo-500 mb-3">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z" />
                                    </svg>
                                </div>
                                <h3 class="font-bold text-lg text-gray-800 mb-2">Live Updates</h3>
                                <p class="text-gray-600 text-sm">Real-time information through our integrated APIs and
                                    community.</p>
                            </div>
                        </div>
                    </div>

                    <!-- Team Section -->
                    <div class="mb-12">
                        <div class="flex items-center gap-3 mb-6">
                            <div class="w-3 h-8 rounded-full bg-blue-500"></div>
                            <h2 class="text-2xl font-bold text-gray-800">Meet The Team</h2>
                        </div>
                        <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
                            <div class="text-center">
                                <div
                                    class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-r from-pink-500 to-purple-600 p-1">
                                    <div class="bg-white w-full h-full rounded-full p-1">
                                        <img src="https://via.placeholder.com/128" alt="Team Member"
                                            class="w-full h-full rounded-full object-cover">
                                    </div>
                                </div>
                                <h3 class="font-bold text-lg">Yug Shah</h3>
                                <p class="text-gray-600">20243255</p>
                                <p class="text-sm text-pink-600 font-medium">Full Stack Developer</p>
                            </div>
                            <!-- Repeat for other team members -->
                            <div class="text-center">
                                <div
                                    class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-r from-blue-500 to-teal-400 p-1">
                                    <div class="bg-white w-full h-full rounded-full p-1">
                                        <img src="https://via.placeholder.com/128" alt="Team Member"
                                            class="w-full h-full rounded-full object-cover">
                                    </div>
                                </div>
                                <h3 class="font-bold text-lg">Neel Mendapara</h3>
                                <p class="text-gray-600">20243175</p>
                                <p class="text-sm text-blue-600 font-medium">Backend Developer</p>
                            </div>
                            <div class="text-center">
                                <div
                                    class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-r from-purple-500 to-indigo-600 p-1">
                                    <div class="bg-white w-full h-full rounded-full p-1">
                                        <img src="https://via.placeholder.com/128" alt="Team Member"
                                            class="w-full h-full rounded-full object-cover">
                                    </div>
                                </div>
                                <h3 class="font-bold text-lg">Yagnik Chavda</h3>
                                <p class="text-gray-600">20243081</p>
                                <p class="text-sm text-purple-600 font-medium">UI/UX Designer</p>
                            </div>
                            <div class="text-center">
                                <div
                                    class="w-32 h-32 mx-auto mb-4 rounded-full bg-gradient-to-r from-green-500 to-emerald-600 p-1">
                                    <div class="bg-white w-full h-full rounded-full p-1">
                                        <img src="https://via.placeholder.com/128" alt="Team Member"
                                            class="w-full h-full rounded-full object-cover">
                                    </div>
                                </div>
                                <h3 class="font-bold text-lg">Bhargav Miyani</h3>
                                <p class="text-gray-600">20247029</p>
                                <p class="text-sm text-green-600 font-medium">Full Stack Developer</p>
                            </div>
                        </div>
                    </div>

                    <!-- Call to Action -->
                    <div class="text-center mt-16">
                        <h3 class="text-xl font-bold text-gray-800 mb-4">Ready to find your perfect college?</h3>
                        <button id="back-to-main"
                            class="px-8 py-3 bg-gradient-to-r from-pink-500 to-purple-600 text-white rounded-xl font-bold hover:from-pink-600 hover:to-purple-700 transition-all shadow-lg">
                            Start Exploring Now
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- All your existing JavaScript remains exactly the same -->
    <script>
        const tabs = document.querySelectorAll('header nav a, aside a.nav-link');
        const sections = {
            Colleges: document.querySelector('#city-search-wrapper'),
            News: document.getElementById('news-section'),
            Reviews: document.getElementById('Review'),
            GetPremium: document.getElementById('GetPremium'),
            Chat: document.getElementById('Chat'),
            AI: document.getElementById('AI')
        };

        tabs.forEach(tab => {
            tab.addEventListener('click', (e) => {
                e.preventDefault();

                // Remove highlight from all tabs
                tabs.forEach(t => t.classList.remove('text-pink-500', 'bg-pink-100'));
                // Add highlight to clicked tab
                tab.classList.add('text-pink-500', 'bg-pink-100');

                // Hide all sections
                Object.values(sections).forEach(section => {
                    if (section) section.classList.add('hidden');
                });

                const selected = tab.dataset.section;
                if (sections[selected]) {
                    sections[selected].classList.remove('hidden');
                }
            });
        });
    </script>

    <script>
        const menuBtn = document.getElementById('menu-toggle');
        const mobileMenu = document.getElementById('mobile-menu');

        menuBtn.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });
    </script>

    <script>
        paypal.Buttons({
            createOrder: function (data, actions) {
                return actions.order.create({
                    purchase_units: [{
                        amount: {
                            value: '6'  // Amount in USD
                        }
                    }]
                });
            },
            onApprove: function (data, actions) {
                return actions.order.capture().then(function (details) {
                    alert('✅ Payment completed by ' + details.payer.name.given_name);
                    // You can redirect or show a success screen here
                });
            }
        }).render('#paypal-button-container');
    </script>

    <script src="/socket.io/socket.io.js"></script>
    <script>
        const socket = io();
        const form = document.getElementById('form');
        const input = document.getElementById('input');
        const messages = document.getElementById('messages');

        let username = prompt('Enter your username:') || 'Anonymous';
        socket.emit('set username', username);

        form.addEventListener('submit', (e) => {
            e.preventDefault();
            if (input.value) {
                socket.emit('chat message', input.value);
                input.value = '';
            }
        });

        socket.on('chat message', (msg) => {
            const item = document.createElement('li');
            item.textContent = `${msg.username}: ${msg.text}`;
            messages.appendChild(item);
            messages.scrollTop = messages.scrollHeight;
        });
    </script>

    <script>
        let map;
        let markers = [];

        const collegeContacts = {
            "Indian Institute of Technology Bombay": "022-2572-2545",
            "University of Mumbai": "022-2654-3000",
            "St. Xavier's College": "022-2262-0661",
            "Delhi University": "011-2766-7857"
        };

        function focusOnCollege(lat, lon, name) {
            if (map) {
                map.setView([lat, lon], 15);
                markers.forEach(marker => {
                    const markerLatLng = marker.getLatLng();
                    if (markerLatLng.lat === lat && markerLatLng.lng === lon) {
                        marker.openPopup();
                    }
                });
            }
        }

        async function fetchColleges() {
            const city = document.getElementById('cityInput').value.trim();
            const resultDiv = document.getElementById('result');
            resultDiv.innerHTML = '<div class="text-center py-8"><div class="inline-block animate-spin rounded-full h-8 w-8 border-t-2 border-b-2 border-pink-500"></div><p class="mt-3 text-gray-600">Searching for colleges...</p></div>';

            const apiKey = '0167fbbf5d454439a3071d713072809e';

            try {
                const geoRes = await fetch(`https://api.geoapify.com/v1/geocode/search?text=${city}&apiKey=${apiKey}`);
                const geoData = await geoRes.json();

                if (!geoData.features || geoData.features.length === 0) {
                    resultDiv.innerHTML = '<div class="bg-white p-6 rounded-xl shadow text-center text-gray-700">City not found. Please try another location.</div>';
                    return;
                }

                const { lon, lat } = geoData.features[0].properties;

                const response = await fetch(`https://api.geoapify.com/v2/places?categories=education.university&filter=circle:${lon},${lat},50000&limit=20&apiKey=${apiKey}`);
                const data = await response.json();

                if (!data.features || data.features.length === 0) {
                    resultDiv.innerHTML = '<div class="bg-white p-6 rounded-xl shadow text-center text-gray-700">No colleges found in this area. Try a different city.</div>';
                    return;
                }

                if (map) {
                    map.remove();
                }

                map = L.map('map').setView([lat, lon], 12);
                L.tileLayer(`https://tile.openstreetmap.org/{z}/{x}/{y}.png`, {
                    attribution: '&copy; OpenStreetMap contributors'
                }).addTo(map);

                markers = [];
                resultDiv.innerHTML = `<h3 class="text-2xl font-bold mb-6 text-gray-800 bg-white p-4 rounded-xl shadow-inner text-center">Colleges near ${city}</h3>`;

                data.features.forEach(college => {
                    const props = college.properties;
                    const coords = college.geometry.coordinates;
                    const name = props.name || "N/A";
                    const phone = collegeContacts[name] || "Not available";

                    resultDiv.innerHTML += `
                <div class="college-card bg-white p-6 rounded-xl shadow-lg text-gray-800 mb-6 border border-gray-100">
                  <h4 class="text-xl font-bold mb-3 text-pink-600">${name}</h4>
                  <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
                    <div>
                        <p class="text-sm text-gray-500">Address</p>
                        <p class="font-medium">${props.address_line1 || 'N/A'}, ${props.address_line2 || ''}</p>
                    </div>
                    <div>
                        <p class="text-sm text-gray-500">Contact</p>
                        <p class="font-medium">${phone}</p>
                    </div>
                    <div>
                        <p class="text-sm text-gray-500">Website</p>
                        <p class="font-medium">${props.website ? `<a href="${props.website}" target="_blank" class="text-blue-600 hover:underline">Visit Website</a>` : 'N/A'}</p>
                    </div>
                    <div>
                        <p class="text-sm text-gray-500">Type</p>
                        <p class="font-medium">${props.categories ? props.categories.join(', ') : 'N/A'}</p>
                    </div>
                  </div>
                  <button onclick="focusOnCollege(${coords[1]}, ${coords[0]}, \`${name}\`)" class="w-full md:w-auto px-6 py-2 bg-gradient-to-r from-pink-500 to-purple-500 text-white rounded-lg hover:from-pink-600 hover:to-purple-600 transition-all">
                    Show on Map
                  </button>
                </div>
              `;

                    const marker = L.marker([coords[1], coords[0]])
                        .addTo(map)
                        .bindPopup(`<b class="text-pink-600">${name}</b><br>${props.address_line1 || ''}`);
                    markers.push(marker);
                });
            } catch (error) {
                console.error(error);
                resultDiv.innerHTML = '<div class="bg-white p-6 rounded-xl shadow text-center text-red-500">An error occurred while fetching data. Please try again.</div>';
            }
        }

        function fetchCollegeNews() {
            const apiKey = "pub_81559ded11d25191d0aa202882e07a95f57c8";
            const endpoint = `https://newsdata.io/api/1/news?apikey=${apiKey}&q=college&language=en`;
            const container = document.getElementById("news-container");

            // Show loading state
            container.innerHTML = `
        <div class="col-span-2 text-center py-10">
            <div class="inline-block animate-spin rounded-full h-8 w-8 border-t-2 border-b-2 border-pink-500"></div>
            <p class="mt-3 text-gray-600">Loading news articles...</p>
        </div>
    `;

            fetch(endpoint)
                .then(response => {
                    if (!response.ok) throw new Error('Network response was not ok');
                    return response.json();
                })
                .then(data => {
                    container.innerHTML = "";

                    if (data.results && data.results.length > 0) {
                        data.results.slice(0, 6).forEach(article => {
                            const newsCard = document.createElement("div");
                            newsCard.className = "bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition-all";
                            newsCard.innerHTML = `
                        <h3 class="text-xl font-bold mb-3">
                            <a href="${article.link}" target="_blank" class="hover:text-pink-600 transition-colors">
                                ${article.title || "No title available"}
                            </a>
                        </h3>
                        <p class="text-gray-600 mb-4">${article.description || "No description available."}</p>
                        <div class="flex justify-between items-center text-sm text-gray-500">
                            <span>${article.source_id || "Unknown source"}</span>
                            <span>${article.pubDate ? new Date(article.pubDate).toLocaleDateString() : ""}</span>
                        </div>
                    `;
                            container.appendChild(newsCard);
                        });
                    } else {
                        container.innerHTML = `
                    <div class="col-span-2 text-center py-10">
                        <p class="text-gray-500">No news articles found at this time.</p>
                    </div>
                `;
                    }
                })
                .catch(error => {
                    console.error("Error fetching news:", error);
                    container.innerHTML = `
                <div class="col-span-2 text-center py-10">
                    <p class="text-red-500">Failed to load news. Please try again later.</p>
                </div>
            `;
                });
        }

        // Replace your existing news-link event listener with this:
        document.querySelector('[data-section="News"]').addEventListener("click", (e) => {
            e.preventDefault();
            const newsSection = document.getElementById("news-section");
            const newsContainer = document.getElementById("news-container");

            // Show news section
            document.querySelectorAll('main > section').forEach(section => {
                section.classList.add('hidden');
            });
            newsSection.classList.remove('hidden');

            // Only fetch if container is empty or has loading message
            if (newsContainer.innerHTML === "" || newsContainer.innerHTML.includes("Loading")) {
                fetchCollegeNews();
            }
        });

        document.getElementById("news-link").addEventListener("click", (e) => {
            e.preventDefault();
            document.getElementById("city-search-wrapper").style.display = "none";
            document.getElementById("news-section").classList.remove("hidden");
            document.getElementById("news-section").scrollIntoView({ behavior: "smooth" });
            if (document.getElementById("news-content").innerHTML.trim() === "Loading news...") {
                fetchCollegeNews();
            }
        });

        document.getElementById("colleges-link").addEventListener("click", (e) => {
            e.preventDefault();
            document.getElementById("city-search-wrapper").style.display = "block";
            document.getElementById("news-section").classList.add("hidden");
            document.getElementById("search-section").scrollIntoView({ behavior: "smooth" });
        });

        document.getElementById("menu-toggle").addEventListener("click", () => {
            const menu = document.getElementById("mobile-menu");
            menu.classList.toggle("hidden");
        });
    </script>

    <script>
        document.getElementById('prefForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const data = {
                stream: document.getElementById('stream').value,
                state: document.getElementById('state').value,
                maxFee: document.getElementById('maxFee').value,
                preferences: {
                    placement: +document.getElementById('placement').value,
                    infrastructure: +document.getElementById('infrastructure').value,
                    faculty: +document.getElementById('faculty').value,
                    academic: +document.getElementById('academic').value
                }
            };

            const res = await fetch('/api/recommend', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            });

            const colleges = await res.json();
            const resultDiv = document.getElementById('results');
            resultDiv.innerHTML = '<h2>Top Colleges</h2>' + colleges.map(college => `
                <div class="college bg-white/90 backdrop-blur-sm rounded-2xl shadow-lg p-6 mb-6 transition-transform duration-300 hover:scale-[1.02] hover:shadow-2xl border border-gray-200">
  <h3 class="text-xl font-bold text-pink-600 mb-2">${college.College_Name}</h3>
  <p class="text-gray-700 mb-1">${college.State} | ${college.Stream}</p>
  <p class="text-gray-700 mb-1">UG Fee: ₹${college.UG_fee} | Rating: ${college.Rating}</p>
  <p class="text-gray-700 mb-1">
    Placement: ${college.Placement} |
    Infra: ${college.Infrastructure} |
    Faculty: ${college.Faculty} |
    Academic: ${college.Academic}
  </p>
  <p class="text-gray-800 font-semibold mt-2">
    <strong>Score:</strong> ${college.score.toFixed(2)}
  </p>
</div>

            `).join('');
        });
    </script>

    <script>
        // Handle about button clicks
        document.getElementById('about-link').addEventListener('click', function (e) {
            e.preventDefault();
            openAbout();
        });

        document.getElementById('mobile-about-link').addEventListener('click', function (e) {
            e.preventDefault();
            openAbout();
        });

        document.getElementById('close-about').addEventListener('click', function (e) {
            e.preventDefault();
            closeAbout();
        });

        document.getElementById('home-link').addEventListener('click', function (e) {
            e.preventDefault();
            closeAbout();
        });

        document.getElementById('mobile-home-link').addEventListener('click', function (e) {
            e.preventDefault();
            closeAbout();
        });

        function openAbout() {
            document.querySelector('.sidebar').classList.add('open');
            document.querySelector('.main-content').classList.add('collapsed');
            document.querySelector('.about-content').classList.add('open');
        }

        function closeAbout() {
            document.querySelector('.sidebar').classList.remove('open');
            document.querySelector('.main-content').classList.remove('collapsed');
            document.querySelector('.about-content').classList.remove('open');
        }
    </script>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const mainContent = document.getElementById('main-content');
            const aboutContent = document.getElementById('about-content');

            // Show About Section
            document.querySelectorAll('.about-link').forEach(link => {
                link.addEventListener('click', function (e) {
                    e.preventDefault();

                    // First hide main content
                    mainContent.classList.add('hidden');

                    // After main content hides, show about content
                    setTimeout(() => {
                        aboutContent.classList.remove('hidden');
                    }, 250); // Matches the transition duration
                });
            });

            // Back buttons
            document.querySelectorAll('#back-button, #back-to-main').forEach(button => {
                button.addEventListener('click', function (e) {
                    e.preventDefault();

                    // First hide about content
                    aboutContent.classList.add('hidden');

                    // After about content hides, show main content
                    setTimeout(() => {
                        mainContent.classList.remove('hidden');
                    }, 250); // Matches the transition duration
                });
            });
        });
    </script>

    <script src="../animation.js"></script>
</body>

</html>